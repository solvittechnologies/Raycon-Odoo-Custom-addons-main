<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="assets_frontend" inherit_id="website.assets_frontend" name="Contract Manager">
      <xpath expr="." position="inside">
          <link rel='stylesheet' href='/website_contract/static/src/css/website_contract.less'/>
          <script type="text/javascript" src="/website_contract/static/src/js/website_contract.js"></script>
      </xpath>
    </template>
    
    <template id="assets_backend" inherit_id="website.assets_backend" name="Website Contract Backend Assets">
      <xpath expr="." position="inside">
          <link rel='stylesheet' href='/website_contract/static/src/css/sale_subscription_backend.less'/>
      </xpath>
    </template>


    <!-- Chatter templates -->
    <template id="chatter_change_contract">
        <div>&amp;bull; <b>Template</b>:<t t-esc="msg_before[0]"/> &amp;rarr; <t t-esc="msg_after[0]"/></div>
        <div>&amp;bull; <b>Recurring Price</b>:<t t-esc="msg_before[1]"/> &amp;rarr; <t t-esc="msg_after[1]"/></div>
        <div>&amp;bull; <b>Invoicing Period</b>:<t t-esc="msg_before[2]"/> &amp;rarr; <t t-esc="msg_after[2]"/></div>
    </template>

    <template id="chatter_add_option">
        <div>Option added by customer</div>
        <div>&amp;bull; <b>Option</b>: <t t-esc="new_option.product_id.product_tmpl_id.name"/></div>
        <div>&amp;bull; <b>Price</b>: <t t-esc="price"/></div>
        <div>&amp;bull; <b>Sale Order</b>: None</div>
    </template>

    <template id="chatter_remove_option">
        <div>Option removed by customer</div>
        <div>&amp;bull; <b>Option</b>: <t t-esc="remove_option.product_id.product_tmpl_id.name"/></div>
    </template>

    <template id="chatter_add_paid_option">
        <div>New Option added by Sale Order confirmation</div>
            <t t-foreach="lines" t-as="line">
                <div>&amp;bull; <b>Option</b>: <t t-esc="line.product_id.product_tmpl_id.name"/></div>
                <div>&amp;bull; <b>Sale Order</b>: <t t-esc="line.order_id.name"/></div>
                <div>&amp;bull; <b>Recurring Price</b>: <t t-esc="line.price_unit"/></div>
                <div>&amp;bull; <b>Discount (partial invoicing period)</b>: <t t-esc="line.discount"/></div>
                <div>&amp;bull; <b>Discounted Price</b>: <t t-esc="line.price_subtotal"/></div>
                <br/>
            </t>
    </template>

    <!-- Specific Contract Page -->

    <template id="contract" name="Contract">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container mb64">
                    <div class="row" t-if="user.partner_id == account.partner_id">
                        <div class="col-sm-6">
                            <ol class="breadcrumb mt8 mb0">
                                <li><a href="/my/home"><i class="fa fa-home"/></a></li>
                                <li><a t-attf-href="/my/contract?{{keep_query()}}">My Contracts</a></li>
                                <li>Contract <span t-esc="account.name"/></li>
                            </ol>
                        </div>
                    </div>
                    <div class="oe_structure"/>
                    <div class="row oe_website_contract">
                        <div class="col-md-8">
                            <h3 class="page-header">Contract <span t-field="account.name"/>
                                <small class="pull-right">
                                    <t t-if="account.state == 'open'">
                                        <span class="label label-success"><i class="fa fa-fw fa-check"/> In Progress</span>
                                    </t>
                                    <t t-if="account.state == 'pending'">
                                        <span class="label label-warning"><i class="fa fa-fw fa-refresh"/> To Renew</span>
                                    </t>
                                    <t t-if="account.state == 'close'">
                                        <span class="label label-default"><i class="fa fa-fw fa-remove"/> Closed</span>
                                    </t>
                                </small>
                                <a class="btn btn-info btn-sm css_editable_mode_hidden hidden-print" t-att-href="'/web#model=%s&amp;id=%s&amp;action=%s&amp;view_type=form' % (account._name, account.id, action)" t-ignore="true" groups="sale_contract.group_sale_contract_view">Back</a>
                            </h3>
                            <div t-if="message" t-att-class="'alert ' + message_class" role="alert">
                                <p t-esc="message"/>
                            </div>
                                <div t-field="account.partner_id" t-field-options='{
                                    "widget": "contact",
                                    "fields": [
                                        "name",
                                        "address",
                                        "phone",
                                        "email"]}'/>
                                <div t-if="user.partner_id == account.partner_id">
                                    <a class="small" t-attf-href="/my/account?redirect=/my/contract/#{account.id}/#{account.uuid}">(Wrong address?)</a>
                                </div>
                            <div class="oe_structure"/>
                            <table class="table mt32" id="wc-account-table">
                                <thead>
                                    <tr class="active"><th/><th/><th>Quantity</th><th>Unit Price</th><th>Subtotal</th></tr>
                                </thead>
                                <tfoot>
                                    <tr>
                                        <td/><td/>
                                        <td colspan="2">
                                            <p class="mb0">Subtotal:</p>
                                            <p class="mb0" t-if="user.partner_id == account.partner_id">
                                                <t t-if="account.partner_id.country_id">
                                                <a class="small" t-attf-href="/my/account?redirect=/my/contract/#{account.id}/#{account.uuid}">(Not in <span t-field="account.partner_id.country_id" data-oe-readonly="1"/>?)</a>
                                                </t>
                                                Taxes:
                                            </p>
                                            <p class="mb0" t-if="user.partner_id != account.partner_id">
                                                <abbr title="You need to be logged in as this contract's customer to change country">Taxes</abbr>:
                                            </p>
                                            <p class="mb0"><strong>Total:</strong></p>
                                        </td>
                                        <td>
                                            <p class="mb0" t-field="account.recurring_total" t-field-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/>
                                            <p class="mb0" t-field="account.recurring_amount_tax" t-field-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/>
                                            <strong><p class="mb0" t-field="account.recurring_amount_total" t-field-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></strong>
                                        </td>
                                    </tr>
                                </tfoot>
                                <tbody>
                                    <t t-if="not account.template_id">
                                        <tr t-foreach="account.recurring_invoice_line_ids" t-as="line">
                                            <td class="line-description"><span t-field="line.name"/></td>
                                            <td/>
                                            <td><t t-esc="line.quantity"/> <span t-field="line.uom_id" data-oe-readonly="1"/></td>
                                            <td t-if="not line.discount"><span t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                            <td t-if="line.discount">
                                                <s t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/><br/>
                                                <div><strong t-esc="line.price_unit*(100.0-line.discount)/100.0" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></div>
                                            </td>
                                            <td><span t-esc="line.price_subtotal" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                            <td/>
                                        </tr>
                                    </t>

                                    <tr t-foreach="account.recurring_mandatory_lines" t-as="line">
                                        <td class="line-description"><span t-field="line.name"/></td>
                                        <td/>
                                        <td><t t-esc="line.quantity"/> <span t-field="line.uom_id" data-oe-readonly="1"/></td>
                                        <td t-if="not line.discount"><span t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                        <td t-if="line.discount">
                                            <s t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/><br/>
                                            <div><strong t-esc="line.price_unit*(100.0-line.discount)/100.0" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></div>
                                        </td>
                                        <td><span t-esc="line.price_subtotal" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                    </tr>
                                    <tr t-foreach="account.recurring_option_lines" t-as="line">
                                        <td class="line-description"><span t-field="line.name"/></td>
                                        <td>
                                            <a data-toggle="modal"
                                               t-attf-data-target="#wc-modal-remove-#{line.get_template_option_line().id}"
                                               href=""
                                               t-if="(line.get_template_option_line().portal_access in ['both'] or is_salesman) and account.state!='close'">
                                                Remove
                                            </a>
                                            <t t-call="website_contract.modal_remove"/>
                                        </td>
                                        <td><t t-esc="line.quantity"/> <span t-field="line.uom_id" data-oe-readonly="1"/></td>
                                        <td t-if="not line.discount"><span t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                        <td t-if="line.discount">
                                            <s t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/><br/>
                                            <div><strong t-esc="line.price_unit*(100.0-line.discount)/100.0" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></div>
                                        </td>                                        <td><span t-esc="line.price_subtotal" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                    </tr>
                                    <tr t-foreach="account.recurring_custom_lines" t-as="line">
                                        <td class="line-description"><span t-field="line.name"/></td>
                                        <td/>
                                        <td><t t-esc="line.quantity"/> <span t-field="line.uom_id" data-oe-readonly="1"/></td>
                                        <td t-if="not line.discount"><span t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                        <td t-if="line.discount">
                                            <s t-esc="line.price_unit" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/><br/>
                                            <div><strong t-esc="line.price_unit*(100.0-line.discount)/100.0" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></div>
                                        </td>
                                        <td><span t-esc="line.price_subtotal" t-options='{"widget": "monetary", "display_currency": pricelist.currency_id}'/></td>
                                    </tr>
                                </tbody>
                            </table>
                            <div t-if="account.state in ['pending','close']" t-attf-class="alert alert-#{'warning' if account.state=='pending' else 'danger'}" role="alert" id="payment_message">
                                <p t-if="account.state == 'pending'" id='warning_pending'>Your subscription is waiting for payment.</p>
                                <div t-if="payment_mandatory and account.state!='open'">
                                    <p id="wc_warning_closed" t-if="account.state == 'close'">Your subscription is closed.
                                        <t t-if="missing_periods == 1">If you wish to reopen it, you can pay your invoice for the current invoicing period.</t>
                                        <t t-if="missing_periods &gt; 1">If you wish to reopen it, the <t t-esc="missing_periods"/> missing payments (from <span t-field="account.recurring_next_date"/> to this day) will be automatically processed.</t>
                                    </p>
                                    <form t-if="acquirers" method="post" class="mt16 clearfix" id="wc-payment-form" t-attf-action="/my/contract/payment/#{account.id}/#{account.uuid}">
                                        <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <div class="form-group">
                                            <select class="form-control" name="pay_meth">
                                                <option t-if="account.payment_token_id" value="-2">Current: <span t-esc="account.payment_token_id.name"/></option>
                                                <option value="-1" t-att-selected="'selected' if not account.payment_token_id else ''">New</option>
                                                <optgroup label="All Payment Methods">
                                                    <t t-foreach="part_pms" t-as="pm">
                                                        <option  t-att-value="pm.id" t-esc="pm.name"/>
                                                    </t>
                                                </optgroup>
                                            </select>
                                        </div>
                                        <div class="clearfix"></div>
                                        <button type="submit" t-attf-class="mt8 btn btn-primary btn-sm pull-right contract-submit #{'hidden' if not account.payment_token_id else ''}">Pay Now</button>
                                    </form>
                                    <div id="new_payment_method" t-if="account.state in ['pending', 'close']" t-attf-class="#{'hidden' if account.payment_token_id else ''} clearfix">
                                        <div t-if="error_message" class="alert alert-danger">
                                          <t t-foreach="error_message" t-as="err"><t t-esc="err"/><br /></t>
                                        </div>
                                        <div t-if='len(acquirers)>1'>
                                            <label class="control-label" for="pm_acquirer_id">Acquirer</label>
                                            <select name="pm_acquirer_id" class="form-control">
                                                <t t-foreach="acquirers" t-as="acquirer">
                                                    <option t-att-value="acquirer.id" t-esc="acquirer.name"/>
                                                </t>
                                            </select>
                                        </div>
                                        <t t-foreach="acquirers" t-as="acquirer">
                                            <div t-raw="acquirer.form" t-att-data-acquirer-id="acquirer.id" t-attf-class="acquirer #{'hidden' if acquirer!=acquirers[0] else ''}" t-att-data-provider="acquirer.provider" />
                                        </t>
                                    </div>
                                </div>
                            </div>
                            <t t-if="account.recurring_inactive_lines">
                                <h3 class="page-header">Extra Options</h3>
                                <div class="oe_structure"/>
                                <table class="table table-default table-condensed table-hover" id="wc-option-table">
                                    <thead>
                                        <tr class='active'><th>Name</th><th/><th>Price</th></tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="inactive_options" t-as="line">
                                            <t t-if="line.portal_access != 'invisible'">
                                                <td><span t-field="line.name"/></td>
                                                <td>
                                                    <a data-toggle="modal"
                                                       t-attf-data-target="#wc-modal-add-#{line.id}"
                                                       href=""
                                                       t-if="line.portal_access in ['both', 'upgrade'] and account.state!='close'">
                                                        Add
                                                    </a>
                                                    <t t-call="website_contract.modal_add"/>
                                                </td>
                                                <td>
                                                    <span t-esc="line.with_context(pricelist_id=pricelist.id).price" t-options="{'widget': 'monetary', 'display_currency': pricelist.currency_id}"/> / <span t-field="line.uom_id" data-oe-readonly="1"/>
                                                </td>
                                            </t>
                                        </tr>
                                    </tbody>
                                </table>
                            </t>
                            <t t-if="account.description">
                                <div class="oe_structure"/>
                                <h3 class="page-header">Terms and conditions</h3>
                                <div t-field="account.description"/>
                            </t>
                        </div>
                        <div class="col-md-3 col-md-offset-1">
                            <div class="oe_structure"/>
                            <div>
                                <h3 class="page-header">Your Plan</h3>
                                <div>Current plan: <span t-field="template.name"/></div>
                                <div>Reference: <span t-field="account.code"/></div>
                                <div>Start date: <span t-field="account.date_start"/></div>
                                <div t-if="account.date">Valid until: <span t-field="account.date"/></div>
                                <div>Billing:
                                    Every
                                    <t t-esc="account.recurring_interval"/>
                                    <t t-if="account.recurring_rule_type=='daily'">
                                        day(s)
                                    </t>
                                    <t t-if="account.recurring_rule_type=='weekly'">
                                        week(s)
                                    </t>
                                    <t t-if="account.recurring_rule_type=='monthly'">
                                        month(s)
                                    </t>
                                    <t t-if="account.recurring_rule_type=='yearly'">
                                        year(s)
                                    </t>
                                </div>
                                <div>Next invoice: <span t-field="account.recurring_next_date"/></div>
                                <a t-if="account.state=='open' and account.template_id and display_change_plan" class="mt8 btn btn-primary btn-sm" t-att-href="'/my/contract/'+str(account.id)+'/change'+'?uuid='+str(account.uuid)">Change plan</a>
                            </div>
                            <div t-if="account.user_id">
                                <a name="wc-your-contact"/>
                                <h3 class="page-header">Contract Manager</h3>
                                <div t-field="account.user_id" t-field-options='{
                                    "widget": "contact",
                                    "fields": [
                                        "name",
                                        "phone",
                                        "email"]}'/>
                            </div>
                            <div t-if="display_close" >
                                <h3 class="page-header">Close your account</h3>
                                <p>If you close your account, it will remain active until your next invoice date (<span id="wc-next-invoice" t-field="account.recurring_next_date"/>). After this date, you can reopen your account up to <span t-esc="account.recurring_interval"/>
                                            <t t-if="account.recurring_rule_type=='daily'">
                                                day(s)
                                            </t>
                                            <t t-if="account.recurring_rule_type=='weekly'">
                                                week(s)
                                            </t>
                                            <t t-if="account.recurring_rule_type=='monthly'">
                                                month(s)
                                            </t>
                                            <t t-if="account.recurring_rule_type=='yearly'">
                                                year(s)
                                            </t>
                                by paying your invoice.</p>
                                <a class="mt8 btn btn-default btn-sm" href="" id="wc-close-account" data-toggle="modal" data-target="#wc-modal-close">Close your account</a>
                            </div>
                            <div class="oe_structure"/>
                        </div>
                    </div>
                    <div class="oe_structure"/>
                </div>
            </div>

            <div class="modal fade" id="wc-modal-close" t-if="display_close">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h4 class="modal-title">Close your contract</h4>
                        </div>
                        <form method="post" t-attf-action="/my/contract/#{account.id}/close/?uuid=#{account.uuid}">
                            <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="modal-body">
                                <p>If you confirm, you contract will be closed right away. Your current invoicing period is valid until <span t-field="account.recurring_next_date"/>.</p>
                                <p>We always listen to our customer. Could you specify the reason for cancelling your subscription?</p>
                                <div class="form-group">
                                    <select class="form-control" name="close_reason_id">
                                        <t t-foreach="close_reasons" t-as="close_reason">
                                            <option  t-att-value="close_reason.id" t-esc="close_reason.name"/>
                                        </t>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <textarea class="form-control" name="closing_text" style="width: 100%;" rows="4"></textarea>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                                <button class="btn btn-primary contract-submit">Confirm</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="prepaid" name="Prepaid Contract">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container mb64">
                    <div class="row" t-if="user.partner_id == account.partner_id">
                        <div class="col-sm-6">
                            <ol class="breadcrumb mt8 mb0">
                                <li><a href="/my/home">My Account</a></li>
                                <li>Contract <span t-esc="account.name"/></li>
                            </ol>
                        </div>
                    </div>
                    <div class="oe_structure"/>
                    <div class="row">
                        <div class="col-md-8">
                            <h3 class="page-header">Contract <span t-field="account.name"/>
                                <small class="pull-right">
                                    <t t-if="account.state == 'open'">
                                        <span class="label label-success"><i class="fa fa-fw fa-check"/> In Progress</span>
                                    </t>
                                    <t t-if="account.state == 'pending'">
                                        <span class="label label-warning"><i class="fa fa-fw fa-refresh"/> To Renew</span>
                                    </t>
                                    <t t-if="account.state == 'close'">
                                        <span class="label label-default"><i class="fa fa-fw fa-remove"/> Closed</span>
                                    </t>
                                </small>
                                <a class="btn btn-info btn-sm css_editable_mode_hidden hidden-print" t-att-href="'/web#model=%s&amp;id=%s&amp;action=%s&amp;view_type=form' % (account._name, account.id, action)" groups="website.group_website_publisher" t-ignore="true">View Contract</a>
                            </h3>
                            <div t-field="account.partner_id" t-field-options='{
                                "widget": "contact",
                                "fields": [
                                    "name",
                                    "address",
                                    "phone",
                                    "email"]}'/>

                            <div class="oe_structure"/>
                            <div>
                                <h3 class="page-header">
                                    Activities
                                    <span class="label label-info pull-right">
                                        <strong>Prepaid units:</strong> <span t-esc="account.quantity_max"/>
                                    </span>
                                </h3>
                                <div class="well">
                                    <div class="progress mb4">
                                      <div class="progress-bar" role="progressbar" t-attf-aria-valuenow="#{account.hours_quantity/account.quantity_max*100}" aria-valuemin="0" aria-valuemax="100" t-attf-style="width: #{account.hours_quantity/account.quantity_max*100}%; min-width:10%">
                                      </div>
                                    </div>
                                    <div class="text-center small">
                                         <span><t t-esc="account.hours_quantity"/> Units Used</span> - <span><t t-esc="account.remaining_hours"/> Units Remaining</span>
                                     </div>
                                 </div>
                                <table class="table mt32">
                                    <thead>
                                        <tr class="active"><th>Date</th><th>Description</th><th>Responsible</th><th>Quantity</th></tr>
                                    </thead>
                                    <tbody>

                                        <tr t-foreach="analytic_lines" t-as="line">
                                            <td><span t-field="line.date"/></td>
                                            <td><span t-field="line.name"/></td>
                                            <td><t t-esc="line.user_id.name"/></td>
                                            <td><t t-esc="'%.2f' % line.unit_amount"/> Unit(s)</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-if="account.description">
                                <div class="oe_structure"/>
                                <h3 class="page-header">Terms and conditions</h3>
                                <div t-field="account.description"/>
                            </t>
                        </div>
                        <div class="col-md-3 col-md-offset-1">
                            <div class="oe_structure"/>
                            <div>
                                <h3 class="page-header">Contract Details</h3>
                                <div>Start date: <span t-field="account.date_start"/></div>
                                <div t-if="account.date">Valid until: <span t-field="account.date"/></div>
                            </div>
                            <div t-if="account.user_id">
                                <a name="wc-your-contact"/>
                                <h3 class="page-header">Sales Representative</h3>
                                <div t-field="account.user_id" t-field-options='{
                                    "widget": "contact",
                                    "fields": [
                                        "name",
                                        "phone",
                                        "email"]}'/>
                            </div>
                            <div class="oe_structure"/>
                        </div>
                    </div>
                    <div class="oe_structure"/>
                </div>
            </div>

            <div class="modal fade" id="wc-modal-close" t-if="display_close">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h4 class="modal-title">Close your contract</h4>
                        </div>
                        <form method="post" t-attf-action="/my/contract/#{account.id}/close/?uuid=#{account.uuid}">
                            <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="modal-body">
                                <p>If you confirm, you contract will be closed right away. Your current invoicing period is valid until <span t-field="account.recurring_next_date"/>.</p>
                                <p>We always listen to our customer. Could you specify the reason for cancelling your subscription?</p>
                                <div class="form-group">
                                    <select class="form-control" name="close_reason_id">
                                        <t t-foreach="close_reasons" t-as="close_reason">
                                            <option  t-att-value="close_reason.id" t-esc="close_reason.name"/>
                                        </t>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <textarea class="form-control" name="closing_text" style="width: 100%;" rows="4"></textarea>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                                <button class="btn btn-primary contract-submit">Confirm</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="modal_remove">
        <div class="modal fade" t-attf-id="wc-modal-remove-#{line.get_template_option_line().id}">
            <div class="modal-dialog">
                <div class="modal-content">
                    <form method="post" t-attf-action="/my/contract/#{account.id}/remove_option/?uuid=#{account.uuid}">
                        <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="modal-header">
                            <h4 class="modal-title">Confirm Option Removal</h4>
                        </div>
                        <div class="modal-body">
                            <p>The following option will be removed from your next invoice:</p>
                            <p class="line-description" t-field="line.name"/>
                            <input name="remove_option_id" type="hidden" t-att-value="line.get_template_option_line().id" />
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                            <button class="btn btn-primary contract-submit">Confirm</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </template>

    <template id="modal_add">
        <div class="modal fade" t-attf-id="wc-modal-add-#{line.id}">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h4 class="modal-title">Confirm Option</h4>
                    </div>
                    <div class="modal-body">
                        <p>The following option will be added to your next invoice:</p>
                        <p class="line-description" t-field="line.name"/>
                        <t t-set="pricelist" t-value="request.website.get_current_pricelist()"/>
                        <p t-if="line.with_context(pricelist_id=pricelist.id).price&gt;0.0 and template.partial_invoice">You will be invoiced for the remaining of this invoicing period (from today to <span t-field="account.recurring_next_date" />).</p>
                    </div>
                    <div class="modal-footer">
                        <form method="post" t-attf-action="/my/contract/#{account.id}/pay_option"
                              t-if="line.with_context(pricelist_id=pricelist.id).price&gt;0.0 and template.partial_invoice">
                            <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <input name="new_option_id" type="hidden" t-att-value="line.id" />
                            <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                            <button class="btn btn-primary contract-submit">Confirm</button>
                        </form>
                        <form method="post" t-attf-action="/my/contract/#{account.id}/add_option/?uuid=#{account.uuid}" t-if="line.with_context(pricelist_id=pricelist.id).price==0.0 or not template.partial_invoice">
                            <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <input name="new_option_id" type="hidden" t-att-value="line.id" />
                            <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                            <button class="btn btn-primary contract-submit">Confirm</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <!--  Modification Pages -->
    <template id="change_template" name="Change your plan">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container">
                    <div class="row">
                        <div class="col-sm-6">
                            <ol class="breadcrumb mt8">
                                <li t-if="user.partner_id == account.partner_id"><a href="/my/home">My Account</a></li>
                                <li><a t-att-href="'/my/contract/'+str(account.id)+'/'+str(account.uuid)">Contract <span t-esc="account.name"/></a></li>
                                <li>Change Your Plan</li>
                            </ol>
                        </div>
                    </div>
                    <h1>Change Your Plan</h1>
                    <p>Your options will be carried to the new plan if they are available.</p>
                    <div class="row oe_website_contract">
                        <t t-set="template" t-value="active_template"/>
                        <t t-call="website_contract.template_panel"/>

                        <t t-foreach="inactive_templates" t-as="template">
                            <t t-if="template.user_selectable and not template.id==account.template_id.id">
                                <t t-call="website_contract.template_panel"/>
                            </t>
                        </t>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="preview_template" name="Preview Template">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container mb32 mt32">
                    <div class="row">
                        <div class="pull-right css_editable_mode_hidden hidden-print">
                              <a class="btn btn-info" t-att-href="'/web#model=%s&amp;id=%s&amp;action=%s&amp;view_type=form' % (template._name, template.id, action)">Back</a>
                        </div>
                        <div class="col-md-4 col-md-offset-4">
                            <div class="panel panel-primary">
                                <div class="panel-heading">
                                    <h4 class="pull-left"><span t-field="template.name" /> <span class="label label-default">Current Plan</span></h4>
                                    <div class="clearfix"/>
                                </div>
                                <div class="panel-body">
                                    <div t-field="template.plan_description" />
                                    <hr/>
                                    <p>
                                        Billing Period: <span t-esc="template.recurring_interval"/>
                                                    <t t-if="template.recurring_rule_type=='daily'">
                                                        day(s)
                                                    </t>
                                                    <t t-if="template.recurring_rule_type=='weekly'">
                                                        week(s)
                                                    </t>
                                                    <t t-if="template.recurring_rule_type=='monthly'">
                                                        month(s)
                                                    </t>
                                                    <t t-if="template.recurring_rule_type=='yearly'">
                                                        year(s)
                                                    </t>
                                    </p>
                                </div>
                                <table class="table wc-template-panel-table">
                                    <tbody>
                                        <tr>
                                            <td><h4>Basic Plan</h4></td>
                                            <td/>
                                        </tr>
                                        <t t-set="pricelist" t-value="request.website.get_current_pricelist()"/>
                                        <tr t-foreach="template.subscription_template_line_ids" t-as="line" class="line-description">
                                            <td class="line-description"><span t-field="line.name"/></td>
                                            <t t-set="price" t-value="line.with_context(pricelist_id=pricelist.id).price"/>
                                            <td><span t-esc="price" t-options="{'widget': 'monetary', 'display_currency': pricelist.currency_id}"/></td>
                                        </tr>
                                        <tr>
                                            <td><h4>Options</h4></td>
                                            <td/>
                                        </tr>
                                        <tr t-foreach="template.subscription_template_option_ids" t-as="line" class="line-description">
                                            <t t-if="line.portal_access != 'invisible'">
                                                <td class="line-description"><span t-esc="line.name"/></td>
                                                <t t-set="price" t-value="line.with_context(pricelist_id=pricelist.id).price"/>
                                                <td><span t-esc="price" t-options="{'widget': 'monetary', 'display_currency': pricelist.currency_id}"/></td>
                                            </t>
                                        </tr>
                                    </tbody>
                                </table>
                                <div class="panel-footer clearfix">
                                    <button type="button" class="btn btn-standard pull-right" disabled="True">Current Plan</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="template_panel">
        <div class="col-md-4">
            <div t-att-class="'panel panel-primary' if (account.template_id.id == template.id) else 'panel panel-default'">
                <div class="panel-heading">
                    <h4 class="pull-left"><span t-esc="template.name" /> <span t-if="account.template_id.id == template.id" class="label label-default">Current Plan</span></h4>
                    <button type="button"
                            class="btn btn-primary pull-right"
                            data-toggle="modal"
                            t-if="template.user_selectable and account.template_id.id != template.id"
                            t-att-data-target="'#contract_confirm_'+str(template.id)">
                        <i class="fa fa-fw fa-arrow-circle-right"></i> Select
                    </button>
                    <div class="clearfix"/>
                </div>
                <div class="panel-body">
                    <div t-raw="template.plan_description" />
                    <hr/>
                    <p>
                        Billing Period: <span t-esc="template.recurring_interval"/>
                                    <t t-if="template.recurring_rule_type=='daily'">
                                        day(s)
                                    </t>
                                    <t t-if="template.recurring_rule_type=='weekly'">
                                        week(s)
                                    </t>
                                    <t t-if="template.recurring_rule_type=='monthly'">
                                        month(s)
                                    </t>
                                    <t t-if="template.recurring_rule_type=='yearly'">
                                        year(s)
                                    </t>
                    </p>
                </div>
                    <table class="table wc-template-panel-table">
                        <tbody>
                            <tr>
                                <td><h4>Basic Plan</h4></td>
                                <td/>
                            </tr>
                            <tr t-foreach="template.subscription_template_line_ids" t-as="line" class="line-description">
                                <td class="line-description"><span t-esc="line.name"/></td>
                                <td><span t-esc="line.with_context(pricelist_id=pricelist.id).price" t-options="{'widget': 'monetary', 'display_currency': pricelist.currency_id}"/></td>
                            </tr>
                            <tr>
                                <td><h4>Options</h4></td>
                                <td/>
                            </tr>
                            <tr t-foreach="template.subscription_template_option_ids" t-as="line" class="line-description">
                                <t t-if="line.portal_access != 'invisible'">
                                    <td class="line-description"><span t-esc="line.name"/></td>
                                    <td><span t-esc="line.with_context(pricelist_id=pricelist.id).price" t-options="{'widget': 'monetary', 'display_currency': pricelist.currency_id}"/></td>
                                </t>
                            </tr>
                        </tbody>
                    </table>
                <div class="panel-footer clearfix">
                    <button type="button" t-if="account.template_id.id == template.id" class="btn btn-standard pull-right" disabled="True">Current Plan</button>
                    <form method="post" t-if="template.user_selectable and account.template_id.id != template.id">
                        <input class="hidden" name="new_template_id" t-att-value="template.id"/>
                        <input class="hidden" name="uuid" t-att-value="account.uuid"/>
                        <input class="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <button type="button" class="btn btn-primary pull-right" data-toggle="modal" t-att-data-target="'#contract_confirm_'+str(template.id)"><i class="fa fa-fw fa-arrow-circle-right"></i> Select</button>
                        <div class="modal fade" t-att-id="'contract_confirm_'+str(template.id)">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h4 class="modal-title">Change Contract</h4>
                                    </div>
                                    <div class="modal-body">
                                        <p>Please ensure that you have selected the correct plan:</p>
                                        <ul>
                                            <li>Plan: <span t-esc="template.name"/></li>
                                            <li>
                                                Billing Period: <span t-esc="template.recurring_interval"/>
                                                <t t-if="template.recurring_rule_type=='daily'">
                                                    day(s)
                                                </t>
                                                <t t-if="template.recurring_rule_type=='weekly'">
                                                    week(s)
                                                </t>
                                                <t t-if="template.recurring_rule_type=='monthly'">
                                                    month(s)
                                                </t>
                                                <t t-if="template.recurring_rule_type=='yearly'">
                                                    year(s)
                                                </t>
                                            </li>
                                        </ul>
                                        <p>These modifications will be active immediately, however your next invoice date will not be changed. Your next invoice will arrive on: <span t-field="account.recurring_next_date"/></p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                                        <a class="btn btn-primary contract-submit">Confirm</a>
                                    </div>
                                </div><!-- /.modal-content -->
                            </div><!-- /.modal-dialog -->
                        </div><!-- /.modal -->
                    </form>
                </div>
            </div>
        </div>
    </template>

    <!-- Modify website_portal templates -->
    <template id="portal_my_home_menu_contract" name="Portal layout : contract menu entry" inherit_id="website_portal.portal_layout" priority="20">
        <xpath expr="//ul[contains(@class,'o_portal_submenu')]" position="inside">
            <li>
                <a href="/my/contract">Contracts</a>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_contract" name="Portal My Home : contract entries" inherit_id="website_portal.portal_my_home" priority="20">
        <xpath expr="//div[contains(@class,'o_my_home_content')]" position="inside">
            <h3 class="page-header">
                <a href="/my/contract">Your Contracts
                    <small class="ml8">
                        <t t-if="contract_count">
                            <span class='badge'><t t-esc="contract_count"/></span>
                        </t>
                        <t t-if="not contract_count">
                            There are currently no contracts for your account.
                        </t>
                    </small>
                </a>
            </h3>
        </xpath>
    </template>

    <template id="portal_my_contracts" name="My Contracts">
        <t t-call="website_portal.portal_layout">
            <h3 class="page-header"></h3>
             <div class="row">
                <h3 class="page-header">
                    Your Contracts
                    <div class="dropdown pull-right mr8">
                        <button id="sortby" class="btn btn-default" type="button" data-toggle="dropdown">
                            <span class="fa fa-sort fa-lg" /> <span t-esc="sortings.get(sortby, {}).get('label', 'Newest')"/>
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="sortby">
                            <li t-foreach="sortings" t-as="option" t-attf-class="#{sortby == option and 'active'}">
                                <a t-att-href="'/my/contract?' + keep_query('date_begin', 'date_end', sortby=option)"><span t-esc="sortings.get(option).get('label')"/></a>
                            </li>
                        </ul>
                    </div>
                    <div class="dropdown pull-right mr8">
                        <button id="filters" class="btn btn-default" type="button" data-toggle="dropdown">
                            <span class="fa fa-filter fa-lg" /> <span t-esc="filters.get(select, {}).get('label', 'All')"/>
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="filters">
                            <li t-foreach="filters" t-as="option" t-att-class="select == option and 'active'">
                                <a t-att-href="'/my/contract?' + keep_query('date_begin', 'date_end', 'sortby', select=option)"><span t-esc="filters.get(option).get('label')"/></a>
                            </li>
                        </ul>
                    </div>
                </h3>
            </div>
            <t t-if="not accounts">
                <p>You don't have any subscriptions yet.</p>
            </t>
            <t t-if="accounts">
                <table class="table table-hover status_table">
                    <thead>
                        <tr class="active">
                            <th>Name</th>
                            <th/>
                            <th></th>
                        </tr>
                    </thead>
                    <t t-foreach="accounts" t-as="account">
                        <tr>
                            <td>
                                <a t-att-href="'/my/contract/'+str(account.id)+'/'+str(account.uuid)+'?'+keep_query()"><t t-esc="account.name"/></a>
                            </td>
                            <td id="contract_state">
                                <t t-if="account.state == 'open'">
                                    <span class="label label-success"><i class="fa fa-fw fa-check"/> In Progress</span>
                                </t>
                                <t t-if="account.state == 'pending'">
                                    <span class="label label-warning"><i class="fa fa-fw fa-refresh"/> To Renew</span>
                                </t>
                                <t t-if="account.state == 'close'">
                                    <span class="label label-default"><i class="fa fa-fw fa-remove"/> Closed</span>
                                </t>
                            </td>
                            <td><span t-esc="account.recurring_total" t-options="{'widget': 'monetary', 'display_currency': account.pricelist_id.currency_id}"/></td>
                        </tr>
                    </t>
                </table>
                <div class="col-md-12 text-right">
                    <t t-call="website.pager"/>
                </div>
            </t>
        </t>
    </template>

    <!-- Modify website_quote templates -->
    <template id="so_quotation" inherit_id="website_quote.so_quotation">
        <xpath expr="//div[@id='modalaccept']" position="before">
            <div class="alert alert-info alert-dismissable" t-if="quotation.project_id and quotation.subscription_id">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&amp;times;</button>
                <t t-set="subscription" t-value="quotation.subscription_id"/>
                A contract is linked to this quotation: <a t-attf-href="/my/contract/#{subscription.id}/#{subscription.uuid}" t-esc="subscription.name"/>.
            </div>
        </xpath>
    </template>

    <template id="contract_pricing" name="Contract Price" inherit_id="website_quote.pricing">
        <xpath expr="//t[@t-as='line']" position="replace">
            <t t-set="recurring_products" t-value="[line.recurring_product for line in layout_category['lines']]"/>
            <t t-set="recurring_products_titles"
               t-value="(layout_category_size == 1 and page_size == 1) and True in recurring_products and False in recurring_products"/>
            <tr t-if="recurring_products_titles" class="active">
                <td colspan="7">Subscription</td>
            </tr>
            <tr t-foreach="layout_category['lines']" t-as="line">
                <t t-if="line.recurring_product">
                    <t t-call="website_contract.order_line_row"/>
                </t>
            </tr>
            <tr t-if="recurring_products_titles" class="active">
                <td colspan="7">One-time Fees <small>- These products will not be part of your subscription</small></td>
            </tr>
            <tr t-foreach="layout_category['lines']" t-as="line">
                <t t-if="not line.recurring_product">
                    <t t-call="website_contract.order_line_row"/>
                </t>
            </tr>
        </xpath>
    </template>

    <template id="order_line_row">
        <td>
            <span t-field="line.name"/>
        </td>
        <td>
            <div id="quote_qty">
                <span t-field="line.product_uom_qty"/>
                <span t-field="line.product_uom"/>
            </div>
        </td>
        <td>
            <div t-foreach="line.tax_id" t-as="tax">
                <t t-esc="tax.name"/>
            </div>
        </td>
        <td>
            <strong t-if="line.discount" class="text-info">
                <t t-esc="line.discount"/>% discount
            </strong>
        </td>
        <td>
              <div t-field="line.price_unit"
                  t-field-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'
                  t-att-style="line.discount and 'text-decoration: line-through' or ''"
                  t-att-class="(line.discount and 'text-danger' or '') + 'text-right'"/>
              <!-- TODO: apply monetary widget formating -->
              <div t-if="line.discount">
                  <t t-esc="'%.2f' % ((1-line.discount / 100.0) * line.price_unit)"/>
              </div>
        </td>
        <td>
            <div class="text-right"
                t-field="line.price_subtotal"
                t-field-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
        </td>
        <td class="text-center">
            <a t-attf-href="./update_line/#{ line.id }/?order_id=#{ quotation.id }&amp;unlink=True&amp;token=#{ quotation.access_token }" class="mb8 js_update_line_json hidden-print" t-if="line.option_line_id">
                <span class="fa fa-trash-o"></span>
            </a>
        </td>
    </template>

</odoo>
